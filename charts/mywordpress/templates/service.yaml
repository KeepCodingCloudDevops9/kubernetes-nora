apiVersion: v1
kind: Service
metadata:
  name: {{ include "mywordpress.fullname" . }}
  labels:
    {{- include "mywordpress.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mywordpress.selectorLabels" . | nindent 4 }}
